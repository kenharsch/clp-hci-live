<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<p>
  <strong>Users:</strong>
<% @group.users.each do |user| %>
        <br>
<% if @group.user.real_name.nil?
                    name = @group.user.nickname
                  elsif @group.user.real_name.created_at < post.created_at
                    name = @group.user.name
                  else
                    name = @group.user.nickname
                end %>
        <td><%= name %></td>
      </tr>
    <% end %>


 
</p>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
